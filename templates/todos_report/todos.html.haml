%h1 TODOs

%h2 TODOs in repository

= line_chart(repository, :todos_per_day, "TODOs")

%h2 Files with most TODOs

= table(repository, :files_with_most_todos, {filename: "File", revisions: "Revisions", todos: "TODOs"})
